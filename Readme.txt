This project to solve the reinforcement learning.

The OpenAi-GYM-HillClimb file is connected to the OpenAI GYM package, I use SARSA and Q-Learning to solve it.

The InvertedPendulum file is to balance the inverted pendulum in a virtual physical environment. The policy used are VI and PI(Value Iteration and Policy Iteration).